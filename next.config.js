/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'blazed.sirv.com',
          'lh3.googleusercontent.com'
        ],
    },
}

module.exports = nextConfig
